const ContentItems = () => {
    const text = "Content";

    return (
    <div className="content-item">
        <button>❤️</button>
        <p>{text}</p>
    </div>
    );
};

export default ContentItems;